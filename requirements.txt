# Các thư viện phụ thuộc cho ứng dụng cào kết quả xổ số (Deeplott Scrapers)
# Cài đặt toàn bộ thư viện bằng lệnh: pip install -r requirements.txt

# Thư viện gửi các yêu cầu HTTP (POST, GET) để lấy dữ liệu từ ketquadientoan.com
requests>=2.28.0

# Thư viện bs4 (BeautifulSoup) dùng để phân tích cú pháp HTML và trích xuất dữ liệu kết quả
beautifulsoup4>=4.11.0

# Thư viện web server Flask để tạo Web Service API
Flask>=2.2.0

# Web server Gateway (WSGI) phục vụ chạy ứng dụng môi trường Production trên Render
gunicorn>=20.1.0

# === Thư viện cho Lotto 535 Ensemble Prediction System ===

# Xử lý dữ liệu dạng bảng và tính toán số học
pandas>=2.0.0
numpy>=1.24.0

# Machine learning (dùng cho các phương pháp thống kê bổ trợ)
scikit-learn>=1.3.0

# === Thư viện cho Firestore Integration ===

# Firebase Admin SDK: đẩy dữ liệu kết quả xổ số lên Cloud Firestore
# Để app có thể đọc real-time thay vì fetch từ GitHub
firebase-admin>=6.0.0
